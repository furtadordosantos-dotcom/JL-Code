{
  "name": "jl-code",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de cursos JL Code",
  "scripts": { "start": "node server.js", "dev": "node --watch server.js", "test": "node --test tests/*.test.js", "build": "node scripts/build-pages.mjs", "pages:dev": "npm run build && wrangler pages dev dist", "pages:deploy": "npm run build && wrangler pages deploy dist" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.16",
    "openai": "^4.77.3"
  }
}
